{
  "name": "wallet-connector",
  "version": "0.0.0",
  "private": true,
  "description": "Solana wallet-connect monorepo (@opindex/wallet-connect)",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "turbo": "^2.9.10",
    "typescript": "^6.0.3"
  }
}
