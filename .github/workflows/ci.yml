name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# Opt JS-based actions (checkout, setup-node, pnpm/action-setup, etc.)
# onto Node 24's runtime. GitHub deprecated Node 20 for actions in
# Sep 2025; this avoids the warning until the action authors ship
# Node 24 builds by default. Safe to remove once that happens.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  ci:
    name: CI / Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [24]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify changeset status
        if: github.event_name == 'pull_request'
        run: pnpm changeset status --since=origin/${{ github.base_ref }}

      - name: Typecheck
        run: pnpm turbo typecheck

      - name: Lint
        run: pnpm turbo lint

      - name: Build
        run: pnpm turbo build

      - name: Test (with coverage)
        run: pnpm turbo test

      - name: Check documentation links
        run: pnpm docs:link-check

      - name: Verify README code examples parse as TypeScript
        run: pnpm readme:check

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-node-${{ matrix.node }}
          path: packages/*/coverage/
          if-no-files-found: ignore
          retention-days: 14

  size:
    name: Bundle size
    # andresz1/size-limit-action diffs the PR head against the base branch and
    # has nowhere to post the comment outside of a pull_request event, so only
    # run this job on PRs. Push events to `main` (post-merge) are skipped.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Stage size-limit wrapper outside the working tree
        # andresz1/size-limit-action does its own `git checkout -f <branch>` to
        # measure both `main` and the PR head. That checkout removes any file
        # not tracked on the target branch — including this wrapper on the very
        # first PR that adds bundle-size monitoring. Copy it to $RUNNER_TEMP so
        # both branch runs can invoke the same wrapper regardless of which
        # commit is checked out.
        run: |
          mkdir -p "$RUNNER_TEMP/sizelimit"
          cp scripts/size-ci.mjs "$RUNNER_TEMP/sizelimit/size-ci.mjs"

      - name: Check bundle size
        uses: andresz1/size-limit-action@v1.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          package_manager: pnpm
          build_script: build
          script: node ${{ runner.temp }}/sizelimit/size-ci.mjs
